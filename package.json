{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "postbuild": "rm -rf  build && cp -R public/build build"
    },
    "devDependencies": {
        "axios": "^1.7.4",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    },
    "dependencies": {
        "@erbelion/vite-plugin-laravel-purgecss": "^0.2.5",
        "laravel-echo": "^2.2.0",
        "pusher-js": "^8.4.0"
    }
}
